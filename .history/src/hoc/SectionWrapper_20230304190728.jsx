import React from 'react';
import { styles } from '../styles';
import { motion } from 'framer-motion';
import { staggerContainer } from '../utils/motion';



const SectionWrapper = (compo) => {
  return (
    <div>SectionWrapper</div>
  )
}

export default SectionWrapper